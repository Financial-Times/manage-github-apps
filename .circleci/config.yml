version: 2

jobs:

  build:
    docker:
      - image: circleci/node:14-browsers

    steps:
      - checkout

      - restore_cache:
          keys:
            - node-v14-{{ arch }}-{{ checksum "package.json" }}
            - node-v14-{{ arch }}-
            - node-v14- # used if checksum fails

      - run: npm install

      - save_cache:
          key: node-v14-{{ arch }}-{{ checksum "package.json" }}
          paths:
            - ~/usr/local/lib/node_modules
            - node_modules/

      - run: npm test

      - store_test_results:
          path: test-results/

      - store_artifacts:
          path: test-results/
